# 🟢 练习题 1：基础题 —— 计算器测试
# 目标：验证你对 @pytest.mark.parametrize 基础语法的掌握（参数对应关系）。
# 题目描述：
# 写一个简单的普通函数 add(a, b)，返回 a + b。
# 编写一个测试函数 test_add。
# 使用参数化机制，一次性测试以下三组数据：
# 1 + 1 = 2
# 10 + 20 = 30
# 5 + (-5) = 0
# 在测试函数中调用 add 并断言结果正确。
import pytest


def add(a, b):
    return a + b


@pytest.mark.parametrize("a,b,result", [(1, 1, 2), (10, 20, 30), (5, -5, 0)])
def test_add(a, b, result):
    assert add(a, b) == result
